#include <stdio.h> //import the standard input output library of functions

int main() // all functions are inside main, which is initialised by calling the type of variable to be returned in the very end
// if int is returned, return 0; at the very end, otherwise initialise main with void - don't need to return anything
{
    int variable1 = 10;
    char variable2;
    // declaring variables 
    // type variable_name = value;
    printf("%s\n", variable2);
    printf("%d\n", variable1);


    return 0;
}

